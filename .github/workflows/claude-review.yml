name: Claude PR Review

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read
  pull-requests: write
  issues: write
  actions: read
  id-token: write

jobs:
  claude-review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Review PR with Claude
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}

          additional_permissions: |
            actions: read

          prompt: |
            Review this pull request thoroughly.

            Check:
            - Bugs
            - Code quality
            - Security issues
            - Error handling
            - Edge cases
            - Missing test cases
            - Test quality
            - Performance
            - Maintainability

            Review the actual changed code.

            For every issue provide:
            - Severity
            - File
            - Line
            - Problem
            - Why it matters
            - Suggested fix

            At the end provide:

            ## Summary

            Status: PASS or CHANGES_REQUESTED

            Critical:
            High:
            Medium:
            Low:

            ## Tests

            Explain whether the existing tests adequately cover
            the changed functionality.
